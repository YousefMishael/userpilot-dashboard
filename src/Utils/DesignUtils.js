import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#363740",
      sideTextColor: "#dde2ff",
      sideHover: "#363749",
      secondary: "#e5e5e5",
      titleColor: "#252733",
    },
  },
  typography: {
    fontFamily: "Mulish",
  },
});
